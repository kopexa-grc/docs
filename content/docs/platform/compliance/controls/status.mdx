---
title: Status-Modelle
description: Einheitliche, automatisch abgeleitete Status-Logik für Kontrollen, Maßnahmen und Nachweise
---

## Warum Status-Modelle?

In klassischen GRC-Tools muss der Status oft **manuell gepflegt** werden: Excel-Spalten, Notizen oder Kommentare. Das führt zu **Inkonsistenzen**, widersprüchlichen Bewertungen und viel Nacharbeit in Audits.

Kopexa geht einen anderen Weg: Der Status wird **automatisch** berechnet, immer auf Basis der Verknüpfungen zwischen Kontrollen, Maßnahmen und Nachweisen. So entsteht eine **lebendige Compliance-Landkarte**. Sie bleibt ohne manuelles Pflegen konsistent und ist für Auditor:innen jederzeit nachvollziehbar.

<Callout>
**Dein Vorteil:** Weniger Verwaltungsarbeit, weniger Diskussionen im Audit, mehr Zeit für die tatsächliche Verbesserung deiner Sicherheitsmaßnahmen.
</Callout>

## Kontroll-Status

Kontrollen sind die „oberste Ebene". Sie definieren, **was** erreicht werden soll. Ihr Status ergibt sich automatisch aus den zugeordneten Maßnahmen.

| Status | Bedeutung | Wann |
|--------|-----------|------|
| **Umgesetzt** | Alle verknüpften Maßnahmen sind wirksam | Mindestens eine aktive Maßnahme verknüpft **und** alle Maßnahmen haben Status *Wirksam* |
| **Teilweise umgesetzt** | Mindestens eine Maßnahme ist nicht voll wirksam | Mindestens eine Maßnahme hat Status *Eingeschränkt wirksam* oder *Nicht wirksam* |
| **Nicht umgesetzt** | Keine Maßnahmen verknüpft | Default für neu angelegte Kontrollen. Sobald Maßnahmen verknüpft werden, schaltet der Status um |
| **Nicht anwendbar** | Kontrolle ist für deinen Scope nicht relevant | Manuell gesetzt, bleibt sticky und wird nicht automatisch überschrieben |

<Callout type="info" title="Nicht anwendbar als bewusste Entscheidung">
*Nicht anwendbar* ist der einzige Kontroll-Status, den du explizit setzt. Damit dokumentierst du im Statement of Applicability (ISO 27001) oder vergleichbaren Reports: „Wir haben diese Anforderung geprüft und sie ist in unserem Scope nicht relevant", z. B. weil eine Technologie nicht im Einsatz ist. Archivierte Maßnahmen an der Kontrolle beeinflussen den Status nicht. Sie werden in der Aggregation ignoriert.
</Callout>

<Mermaid chart={`
flowchart LR
  A[Nicht umgesetzt] -->|Maßnahme verknüpft| B[Teilweise umgesetzt]
  B -->|alle wirksam| C[Umgesetzt]
  C -->|Maßnahme degradiert| B
  A -.->|manuell| D[Nicht anwendbar]
  B -.->|manuell| D
  C -.->|manuell| D

  style C fill:#dcfce7,stroke:#16a34a
  style B fill:#fef3c7,stroke:#f59e0b
  style A fill:#fee2e2,stroke:#ef4444
  style D fill:#e5e7eb,stroke:#6b7280
`} />

**Warum hilfreich?**
- Du erkennst sofort, welche Kontrollen noch offen oder nur teilweise umgesetzt sind.
- Auditor:innen sehen eine klare Story, von „Nicht umgesetzt" (Planung) bis „Umgesetzt" (Nachweis komplett).
- *Nicht anwendbar* verhindert, dass irrelevante Anforderungen dein Reporting aufblähen. Du musst sie dafür nicht löschen.

## Maßnahmen-Status

Maßnahmen (auch TOMs genannt) sind die konkreten Umsetzungen einer Kontrolle. Ihr Status hängt von den hinterlegten Nachweisen und den verknüpften offenen Aufgaben ab.

| Status | Bedeutung | Wann |
|--------|-----------|------|
| **Wirksam** | Maßnahme ist nachweislich wirksam | Alle Nachweise *Bestanden* **und** keine verknüpfte offene Aufgabe ist mehr als 30 Tage überfällig |
| **Eingeschränkt wirksam** | Maßnahme ist teilweise wirksam | Mindestens ein Nachweis nicht *Bestanden* **oder** eine verknüpfte offene Aufgabe ist mehr als 30 Tage überfällig |
| **Nicht wirksam** | Keine Nachweise vorhanden (auch Default für neue Maßnahmen) | Es gibt keinen verknüpften Nachweis |
| **Archiviert** | Maßnahme ist stillgelegt | Manuell archiviert. Wird in Kontroll-Aggregaten und im Trust Center ignoriert |

<Callout type="info" title="Wann eine Aufgabe als überfällig zählt">
Eine offene Aufgabe zieht eine ansonsten wirksame Maßnahme auf *Eingeschränkt wirksam*, wenn ihr Fälligkeitsdatum mehr als **30 Tage** in der Vergangenheit liegt und die Aufgabe noch nicht erledigt ist. Sobald du die Aufgabe schließt oder das Fälligkeitsdatum verlängerst, fällt die Maßnahme zurück auf den nachweisbasierten Status.
</Callout>

### Wann läuft die Statusberechnung?

Der Maßnahmen-Status wird neu berechnet, sobald sich etwas Relevantes ändert:

- Du änderst die Maßnahme selbst (Status, verknüpfte Nachweise, Aufgaben, Kontrollen)
- Ein verknüpfter Nachweis wechselt den Status (z. B. *Bestanden* → *Nicht bestanden*)
- Eine verknüpfte Aufgabe wird angefasst (Fälligkeitsdatum verschoben, Aufgabe geschlossen)
- Ein Dokument wird archiviert (wirkt auf die daran hängenden Nachweise)

Zusätzlich läuft einmal täglich eine **automatische Überprüfung im Hintergrund**, die alle aktiven Maßnahmen neu bewertet. So werden auch rein zeitgetriebene Wechsel erkannt, zum Beispiel „eine verknüpfte Aufgabe ist gerade über die 30-Tage-Schwelle gerutscht", ohne dass jemand die Aufgabe noch einmal anfassen muss.

<Mermaid chart={`
flowchart LR
  A[Nicht wirksam] -->|Nachweis hinzugefügt| B[Eingeschränkt wirksam]
  B -->|alle Nachweise Bestanden| C[Wirksam]
  C -->|Nachweis Bestanden fällt weg<br/>oder Aufgabe 30 Tage überfällig| B
  A -.->|manuell| D[Archiviert]
  B -.->|manuell| D
  C -.->|manuell| D

  style C fill:#dcfce7,stroke:#16a34a
  style B fill:#fef3c7,stroke:#f59e0b
  style A fill:#fee2e2,stroke:#ef4444
  style D fill:#e5e7eb,stroke:#6b7280
`} />

**Warum hilfreich?**
- Du siehst nicht nur, ob eine Maßnahme existiert, sondern ob sie **wirksam nachgewiesen** ist.
- Das schützt vor „Papier-Kontrollen", die zwar dokumentiert, aber nicht überprüfbar sind.
- Archivierte Maßnahmen verfälschen weder die Kontroll-Aggregation noch das Trust Center.

## Nachweis-Lebenszyklus

Nachweise sind der **Beweis**, dass eine Maßnahme tatsächlich umgesetzt ist. Sie verknüpfen Maßnahmen mit Belegen wie Policies, Audit-Logs, Screenshots oder Prozessdokumenten.

In Kopexa hängen Nachweise oft an **Dokumenten**, die selbst einen Lebenszyklus haben (Status, Überprüfung, Versionierung). Dieser Dokument-Lebenszyklus beeinflusst den Nachweis-Status **automatisch**.

### Nachweis-Status

| Status | Bedeutung | Typische Ursache |
|--------|-----------|------------------|
| **Ausstehend** | Nachweis ist angelegt, aber noch nicht geprüft | Neu erstellt |
| **Handlung erforderlich** | Unterliegendes Dokument ist in Überprüfung oder abgelaufen | Dokument abgelaufen / Überprüfung fällig |
| **Bestanden** | Nachweis ist gültig und freigegeben | Dokument veröffentlicht und freigegeben |
| **Nicht bestanden** | Nachweis ist ungültig | Dokument widerrufen, gelöscht oder Überprüfung nicht erfolgreich |

### Wirkungskette

<Mermaid chart={`
flowchart LR
  A[Dokument-Lebenszyklus] --> B[Nachweis-Status]
  B --> C[Maßnahmen-Status]
  C --> D[Kontroll-Status]
`} />

### Beispiel

- Eine Richtlinie (Dokument) läuft in den jährlichen Überprüfungszyklus.
- Das Dokument springt auf den Status **„Überprüfung erforderlich"**.
- Alle Nachweise, die auf dieser Richtlinie basieren, wechseln automatisch auf **„Handlung erforderlich"**.
- Die betroffenen Maßnahmen fallen auf **„Eingeschränkt wirksam"** oder **„Nicht wirksam"**.
- Die zugehörigen Kontrollen schalten von **„Umgesetzt"** auf **„Teilweise umgesetzt"**.

So entsteht eine **durchgehende Kette**: Dokument → Nachweis → Maßnahme → Kontrolle.

### Sonderfall: Dokument archivieren

Wenn du ein Dokument archivierst, werden **alle daran hängenden Nachweise automatisch gelöscht**. Stellst du das Dokument später wieder her, kommen die Nachweise nicht zurück. Die betroffenen Maßnahmen werden sofort neu bewertet und fallen typischerweise auf *Nicht wirksam*, wenn kein anderer Nachweis übrig ist.

Der Archivierungs-Dialog im Frontend weist explizit auf diese Konsequenz hin.

### Warum wichtig?

- **Realistische Nachweise:** Nachweise spiegeln nicht nur einen Upload wider, sondern den echten Gültigkeitsstatus.
- **Früherkennung:** Sobald ein Dokument in Überprüfung ist, erkennt Kopexa automatisch, dass Handlungsbedarf besteht.
- **Audit-Sicherheit:** Auditor:innen sehen, dass Nachweise nicht nur einmalig hochgeladen, sondern kontinuierlich überwacht werden.

<Callout>
**Benefit für dich:** Keine Excel-Erinnerungen mehr nötig. Kopexa erkennt automatisch, wann Nachweise veralten, und markiert deine Kontrollen entsprechend.
</Callout>

## Best Practices

- **Automatisiert statt manuell:** Kein Status-Klicken. Kopexa übernimmt das.
- **Transparente Logik:** Jeder Status ist klar dokumentiert und nachvollziehbar.
- **Audit-Ready:** Eine Prüferin kann jederzeit von der Kontrolle bis zum einzelnen Nachweis zurückverfolgen, wie ein Status zustande kam.
- **Fokus auf Wirksamkeit:** Dein Team investiert Zeit in echte Verbesserungen statt in Status-Pflege.

<Callout>
Mit Kopexa siehst du nicht nur, **was geplant** ist, sondern auch **was nachweislich funktioniert**.
</Callout>

## FAQ

<Accordions type="single">
  <Accordion title="Warum setzt Kopexa auf automatische Status?">
    Damit Compliance **konsistent und auditierbar** bleibt, unabhängig davon, wer im Team pflegt oder überprüft. Manuelle Status-Pflege führt erfahrungsgemäß zu Inkonsistenzen, die im Audit auffallen.
  </Accordion>
  <Accordion title="Kann ich den Status manuell überschreiben?">
    Nur in einem Fall: Du kannst eine Kontrolle auf *Nicht anwendbar* setzen, um zu dokumentieren, dass sie für deinen Scope nicht relevant ist. Alle anderen Status werden **systemisch berechnet**, damit Manipulation und Fehler ausgeschlossen sind. Du steuerst sie indirekt, indem du Maßnahmen pflegst und Nachweise bereitstellst.
  </Accordion>
  <Accordion title="Was unterscheidet einen Nachweis von einem Dokument?">
    Ein Dokument ist eine **Quelle** (z. B. Policy-PDF, Screenshot). Ein Nachweis ist der **Beleg**, dass dieses Dokument zur Umsetzung einer Maßnahme genutzt wird.
  </Accordion>
  <Accordion title="Warum gibt es den Status Nicht anwendbar?">
    Damit du klar zeigen kannst, dass eine Kontrolle im **Audit-Scope irrelevant** ist, ohne sie löschen zu müssen. Das ist Voraussetzung für das Statement of Applicability bei ISO 27001 und reduziert Rückfragen im Audit.
  </Accordion>
  <Accordion title="Warum verändert eine überfällige Aufgabe den Maßnahmen-Status?">
    Eine Aufgabe mit überschrittenem Fälligkeitsdatum zeigt, dass an der Maßnahme akut etwas offen ist und nicht bearbeitet wird. Wenn das länger als 30 Tage so bleibt, ist die ursprüngliche Wirksamkeitsannahme nicht mehr glaubhaft. Also fällt die Maßnahme automatisch auf *Eingeschränkt wirksam*, bis die Aufgabe erledigt oder das Datum verlängert wird.
  </Accordion>
  <Accordion title="Was passiert, wenn ein Admin die Status-Gruppe eines Workflow-Status ändert?">
    Kopexa bewertet Aufgaben nach ihrer **Status-Gruppe** (*In Bearbeitung* oder *Erledigt* / *Abgebrochen*). Verschiebt ein Admin einen Status von einer Gruppe in eine andere, zum Beispiel „Wartet auf Überprüfung" wird neu als *Erledigt* klassifiziert, dann bewertet Kopexa automatisch alle Maßnahmen neu, die Aufgaben in diesem Status haben. Die Änderung an der Status-Gruppe gilt sofort. Die Maßnahmen-Stati folgen wenige Sekunden später.
  </Accordion>
</Accordions>
